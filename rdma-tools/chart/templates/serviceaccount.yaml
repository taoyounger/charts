apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.name | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.name | trunc 63 | trimSuffix "-" }}
